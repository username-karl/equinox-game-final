package com.equinox.game.entities;

import java.awt.Image;

// Extracted into its own file
public class TacticalQ extends Bullet{
    // Currently just uses Bullet constructor
    public TacticalQ(int x, int y, int width, int height, Image img) {
        super(x, y, width, height, img);
    }
    // Add specific behavior for TacticalQ if needed
} 